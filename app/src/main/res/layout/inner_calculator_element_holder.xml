<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
<RelativeLayout
    android:background="@android:drawable/editbox_background"
    android:layout_margin="5dp"
    android:elevation="5dp"
    android:backgroundTint="@color/ashes"
    android:id="@+id/relinnnercalculator"
    android:layout_width="match_parent"
    android:layout_height="80dp">
    <ImageView
        android:visibility="gone"
        android:id="@+id/innerimageinholder"
        android:layout_margin="5dp"
        android:layout_width="80dp"
        android:layout_height="80dp" />
    <TextView
        android:textColor="@color/color_letters"
        android:text="Dc Resistance"
        android:id="@+id/textviewforinner"
        android:layout_toRightOf="@id/innerimageinholder"
        android:layout_width="wrap_content"
        android:textSize="15sp"
        android:ellipsize="marquee"
        android:fontFamily="@font/almendra_display"
        android:marqueeRepeatLimit="marquee_forever"
        android:singleLine="true"
        android:textStyle="bold"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:layout_centerVertical="true"
        android:layout_height="wrap_content" />
</RelativeLayout>

</android.support.constraint.ConstraintLayout>